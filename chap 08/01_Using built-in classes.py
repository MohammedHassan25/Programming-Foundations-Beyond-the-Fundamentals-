# ------------------------------------------------------------------------------------------------------------
# Object-orianted languages ofen make use of classes and objects in the basic syntax of the language itself
# ------------------------------------------------------------------------------------------------------------

# for example

name = [
    "Mohammed",
    "Mohammed",
    "Hassan",
    "Hassan",
    "Mahmoud"
]

# count knows how many times the word is repeated

print(name.count("Mohammed"))


print(name.pop())      # pop gives you the last word on the list
